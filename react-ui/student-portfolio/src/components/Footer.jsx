const Footer = () => {
  return (
    <footer className="container">
      <p>© {new Date().getFullYear()} Student Portfolio. Built with React & Vite.</p>
    </footer>
  );
};
export default Footer;
